
<div class="subjects delete">
	<h2> Delete Game </h2>
	<%= form_for(:game, :url=> {:action => 'destroy', :id => @game.id}) do |f| %>

		<p> Are you sure you want to permanently delete this record? </p>

		<p class="reference-name"><%= @game.id %></p>

		<div class="form-buttons">
			<%= submit_tag("Delete Game") %>
		</div>

	<% end %>
</div>